import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-lastwords',
  imports: [RouterLink],
  templateUrl: './lastwords.html',
  styleUrl: './lastwords.scss',
})
export class Lastwords {}
